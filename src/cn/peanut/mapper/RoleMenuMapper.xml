<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="cn.peanut.mapper.RoleMenuMapper" >
  <sql id="Base_Column_List" >
    role_id, child_menu_id
  </sql>
  <select id="selectByRoleId" resultType="cn.peanut.bean.po.RoleMenuKey" parameterType="java.lang.Integer">
    select case(child_menu_id)
    when 1 THEN '权限管理1'
    when 2 THEN '菜单管理'
    when 3 THEN '模板配置'
    when 4 THEN '模板列表'
    when 5 THEN '首页置顶广告'
    when 6 THEN '首页分类广告'
    else ' '
    end
    AS
    <include refid="Base_Column_List" />
    from role_menu
    where role_id = #{roleId,jdbcType=INTEGER}
  </select>
</mapper>